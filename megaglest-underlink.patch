--- megaglest-3.9.0/source/glest_game/CMakeLists.txt.orig	2013-12-06 13:15:37.900858645 -0200
+++ megaglest-3.9.0/source/glest_game/CMakeLists.txt	2013-12-06 13:16:35.508855815 -0200
@@ -89,6 +89,11 @@ IF(BUILD_MEGAGLEST)
 		SET(EXTERNAL_LIBS ${EXTERNAL_LIBS} ${CURL_LIBRARIES})
 	ENDIF()
 
+	FIND_PACKAGE(OpenSSL REQUIRED)
+	IF(UNIX)
+		SET(EXTERNAL_LIBS ${EXTERNAL_LIBS} ${OPENSSL_LIBRARIES})
+	ENDIF()
+
 	FIND_PACKAGE(GLEW REQUIRED)
 	INCLUDE_DIRECTORIES(${GLEW_INCLUDE_PATH})
 	IF(UNIX)
